[project]
name = "sfkit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Chadwick Balloo", email = "chadwick.balloo@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "duckdb>=1.5.5",
    "httpx>=0.28.1",
    "lxml>=6.1.3",
    "polars>=1.44.2",
]

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]
